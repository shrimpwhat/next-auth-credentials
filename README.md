# Next Auth app example using credentials

This app uses credentials(email + password) for authenticating users. There is also `prisma` for storing users' data, passwords are encrypted with `argon2` algorithm.
